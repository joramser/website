// These come from Tailwind. Probably there's a way to subset Tailwind colors type, if exists.
export type AllowedColors = 'indigo' | 'orange' | 'rose' | 'emerald' | 'amber' | 'yellow' | 'sky' | 'cyan';
